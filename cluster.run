model cluster.mod;
param inputfile symbolic;


printf("m:");
read m <-;
printf("inputfile:");
read inputfile<-;
printf("k:");
read k <-;
read {i in 1..m}(A[i,1], A[i,2]) < (inputfile);
show;

option solver cplex;
solve;
display x;
display x>output.dat;
